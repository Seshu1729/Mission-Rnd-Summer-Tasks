#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void first_non_repeating_number(int *input, int index)
{

}

int main()
{
	int input[10] = { 1, 2, 3, 1, 4, 5, 3, 4, 2, 5 }, input_size;
	for (int index = 0; index < input_size;index++)
		first_non_repeating_number(input, index);
	getchar();
	return 0;
}
